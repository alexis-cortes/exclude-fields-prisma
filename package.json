{
  "name": "exclude-fields-prisma",
  "version": "1.0.0",
  "description": "Alternative code solution to exclude fields in a prisma query",
  "main": "index.js",
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "scripts": {
    "start": "node index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "exclude in prisma",
    "prismajs",
    "prisma"
  ],
  "author": "Alexis Cortes",
  "license": "ISC",
  "devDependencies": {
    "prisma": "^3.9.2"
  },
  "dependencies": {
    "@prisma/client": "^3.9.2"
  }
}
